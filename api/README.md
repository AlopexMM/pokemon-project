# Preparar el ambiente

Tener instalado Python 3.9 en adelante, nos posicionamos con la consola en la carpeta y corremos

```powershell

python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```